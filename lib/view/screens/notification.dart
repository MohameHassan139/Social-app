import 'package:flutter/material.dart';

class NotficationScreen extends StatelessWidget {
  const NotficationScreen({key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
          child: Text(
        'Notfication Screen',
        style: TextStyle(fontSize: 30),
      )),
    );
  }
}
